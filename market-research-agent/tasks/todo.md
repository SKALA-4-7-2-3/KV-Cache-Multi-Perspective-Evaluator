# 시장조사 에이전트 작업

- [x] 1. 입력 파서 / 내부 모델
  - 수용: 실제 MD에서 기술 2개·근거 2개·6/10/5 한도, 출처 상태를 추출.
  - 수용: 중복·잘못된 한도·깨진 근거 연결을 진단.
  - 검증: parser unittest 및 실제 파일 직접 파싱.
- [x] 2. 예산 / 검색·원문 도구
  - 수용: 시도 단위 예산, 인증·일시적 오류 구분, URL별 부분 실패.
  - 수용: 출처 메타데이터와 스냅샷 보존.
  - 검증: 가짜 HTTP 응답으로 외부 경계 계약 검사.
- [x] 3. 평가 / Graph
  - 수용: 두 기술×6항목, 인용 검사, 최대 1회 보완, 부분 결과 보존.
  - 수용: fixture와 live 제공자에 동일한 Graph 적용.
  - 검증: 결과 중심 단위·Graph 통합 테스트.
- [x] 4. CLI / MD / 캐시 / 부모 어댑터
  - 수용: parse·fixture·live 모드, MD와 상태 저장, 명시적 스냅샷 재사용.
  - 수용: 부모에 market 변경분만 반환.
  - 검증: CLI 끝까지 실행, 출력 검토, 전체 신규 테스트.
- [x] 5. 실제 연결 가능 여부 확인 / 문서 / 최종 검토
  - 수용: 제공된 키로 가능한 API 연결 검증 또는 구체적 제한 기록.
  - 수용: 실행법·구현 범위·검증 결과 문서화.
  - 검증: 코드 검토, compileall, Git 변경 범위 확인.

## 검증 기록 — 2026-09-21

- 신규 unittest 33개 통과. parser, HTTP 경계, 설치된 OpenAI SDK 구조화 출력, 예산·재시도, Graph, MD·재사용을 검사.
- `compileall -q market_agent` 통과.
- `Downloads/input.md` 직접 파싱: SW-01/HW-01, E-SW-001/E-HW-001, 검색 6/원문 10/LLM 5, 경고 없음.
- 실제 입력으로 fixture 전체 실행: 12개 미확인 평가, 보완 1회, 가상 검색 4/원문 2/LLM 2, 실제 API 호출 0회.
- `market_agent/outputs/fixture_demo/market_report.md` 내용 확인, 동일 실행의 명시적 재사용 확인.
- 자체 코드 검토에서 모델 응답 오류의 예외 처리와 주입 예산 상한 누락을 발견하여 회귀 테스트 후 수정.
- API 키는 값 없이 존재 여부만 확인. OpenAI 설정 있음, Tavily 설정 없음. 실제 API 호출은 수행하지 않음.
- 기존 실습 노트북·입력 원본·공유 보고서·환경 파일은 수정하지 않음.

## 키 설정 후 후속 검증 — 완료 기록 추가

- [x] 실제 Tavily Search/Extract와 OpenAI 평가를 연결해 입력 한도 안에서 실행.
- [x] 주요 인용 원문과 시장 주장·출처 범위·발행일을 대조하고 확인 한계를 기록.
- [ ] 팀의 부모 Graph 구현 시 ID reducer, 역할별 예산, checkpoint, 총 페이지 집계 연결.

- 최초 실행: 검색 4/원문 6/LLM 2. API 연결 성공, 무관한 후보 유입 발견.
- 관련성 필터·검색어·미확인 표현 수정 후: 검색 4/원문 10/LLM 2. 원문 9개 확보, 조회 실패 1개, 자동 평가 fact 3/inference 2/unknown 7.
- 수정 후 테스트 35개 및 compileall 통과. 비밀키·입력 원본은 수정하지 않음.
- 세부 출처 검토와 논문/제품 동일성의 남은 한계: `market_agent/LIVE_VALIDATION.md`.

## 출력 품질 수정 작업 — 구현·재실행 완료

2026-09-21 확정한 계획의 필수 수정과 재실행을 2026-09-22에 완료했다. R7의 상세 이력 확장은 선택 작업으로 일부만 구현했다. 상세 설계는 [plan.md](plan.md), 전달 양식은 [MARKET_HANDOFF_TEMPLATE.md](../docs/MARKET_HANDOFF_TEMPLATE.md)를 따른다. 파일 경로는 별도 표시가 없으면 `market_agent/` 기준이다.

**최신 출력 범위:** 시장성 SW/HW 담당 항목과 실제 인용 근거를 통합 에이전트에 전달하는 MD 한 개. 전체 보고서·실행 로그·개발 안내는 전달물에 포함하지 않는다. 우선 순서는 **R1 → R2 → R3 → R8 → R4 → R5 → R6 → R9**, R7은 선택적인 내부 진단 작업이다.

### R1. 기준 결과와 회귀 사례 정의 — P0 / S

- [x] 이번 실행에서 발견한 잘못된 동일성 판정, 모호한 unknown, 조회 실패·예산 소진, 기술군 일반화 사례를 고정된 검토 사례로 기록한다.
- 수용 기준:
  - [x] 각 사례에 입력 근거, 기존 결과, 허용할 결과 범위와 금지할 결론이 있다.
  - [x] 실제 결과와 가상 테스트 응답을 구분하며 원본 실행 기록을 보존한다.
  - [x] 테스트 자료는 키·환경 파일을 포함하지 않고 외부 호출 없이 읽힌다.
- 검증: 실제 `run.json`·저장 원문과 사례 대조, fixtures 구조 검사.
- 의존: 없음.
- 예상 파일: `fixtures/review_cases.json`, `tests/test_review_baseline.py`.

### R2. 미확인 원인을 결과에 표시 — P0 / M

- [x] 항목별 조사 상태·미확인 원인·다음 조사를 내부에서 관리하고 통합용 항목에는 판단에 필요한 한계만 반영한다.
- 수용 기준:
  - [x] 조사 전, 관련 자료 미확보, 원문 접근 실패, 예산 소진을 각각 구분한다.
  - [x] 12개 평가를 유지하며 unknown마다 간결한 이유를 전달한다. 다음 검색 질문·내부 원인 코드는 제어 데이터로 관리한다.
  - [x] 과거 결과는 버전·기본값 정책으로 읽고, 알 수 없는 과거 상태를 추정하지 않는다.
- 검증: 서로 다른 원인으로 동일한 unknown이 발생하는 회귀 사례, 기존 MD·CLI 입력 호환성.
- 의존: R1.
- 예상 파일: `schemas.py`, `validation.py`, `report.py`, `tests/test_validation.py`, `tests/test_cli.py`.

### R3. 선정 기술과 근거 대상 연결 검사 — P0 / M

- [x] 직접 기술 판정과 관련 기술군 정보를 나누고, 주장별 근거·대상·발췌·검토 상태를 연결한다.
- 수용 기준:
  - [x] `judgment` 본문과 `verdict` 공통 판정을 분리한다. fact를 favorable로 자동 변환하지 않고 목표·조건·근거에 따라 판정한다.
  - [x] 제품군 이름·CXL·KV 양자화 단어만으로 선정 논문의 직접 지원·채택을 fact/exact로 통과시키지 않는다.
  - [x] 같은 기술군의 유효한 정보는 context_findings에 보존하며 선정 기술의 unknown과 공존한다.
  - [x] 사실·추론의 발췌가 원문에 존재하고, 발행자 주장·독립 검증·동일성 미검증이 구분된다. 의미상 검증의 한계도 표시한다.
- 검증: HW 공식 제품 발표와 논문 구현이 연결되지 않은 사례, 일반 KV 지원만 있는 사례, 올바른 직접 연결 사례, 조작된 발췌 사례. 설치된 OpenAI SDK의 구조화 출력 검사.
- 의존: R2.
- 예상 파일: `schemas.py`, `prompts.py`, `validation.py`, `tests/test_validation.py`, `tests/test_providers.py`.

### R8. 통합용 시장 담당 항목만 출력 — P0 / M

- [x] 설계보고서 9절 ‘관점별 평가 → 시장성’에 들어갈 SW/HW 항목을 `market_handoff.md`로 전달한다.
- 수용 기준:
  - [x] 두 기술의 6개 항목마다 평가 내용·공통 판정·근거 성격/범위·조건/미확인·근거 ID를 유지한다. 판정과 fact/inference를 혼동하지 않는다.
  - [x] 전달물은 담당 항목과 실제 인용한 출처만 포함한다. 전체 SUMMARY·배경·다른 역할 평가·종합 시사점·로그·테스트 결과·다음 개발 작업·미인용 출처가 없다.
  - [x] `--output` 폴더에는 전달 MD 한 개만 생성한다. 최종 상태·원문·해시·재사용 정보는 별도 내부 `.cache/`에 저장하고 Git에서 제외한다. 상세 회차 저장은 R7 선택 기능이다.
  - [x] MD만 전달해도 실제 인용 출처의 제목·발행자·날짜·URL·공개 원문 위치·접근 범위·짧은 발췌를 확인할 수 있다. 본문과 출처 표의 인용 ID가 양방향으로 일치한다.
  - [x] 입력 v0.1을 유지하고 내부 출력 v0.2를 도입한다. `--reuse`는 식별값·버전·저장 자료 무결성을 검사하며 API 0회, 불일치 시 명시적 오류를 반환한다. 구형 결과는 새 판정으로 자동 승격하지 않는다.
  - [x] 부모에는 기존 market·새 문서·근거·오류 변경분 계약을 유지하고 다른 역할을 덮어쓰지 않는다. CLI의 실패 종료 코드와 통합용 본문 판정을 구분한다.
- 검증: 설계보고서 6.1·9절 및 전달 템플릿과 필드 대응 확인, 불필요한 절·로그·출처 제외, 근거 참조 일치, 부분 실패·특수문자·구형 결과, 손상된 캐시·API 없는 재사용, 부모의 다른 역할 결과 보존.
- 의존: R2~R3. 검색 품질 변경 전 먼저 구현 가능.
- 예상 파일: `report.py`, `cli.py`, `.gitignore`, `README.md`, `tests/test_report.py`, `tests/test_cli.py`, `tests/test_graph.py`.

### 체크포인트 A — 판단·근거·전달 계약 (실행 순서상 R4 이전)

- [x] R1~R3·R8의 회귀 사례 통과, 기존 테스트 회귀 없음.
- [x] ‘선정 기술은 미확인 + 기술군 정보는 확보’ 예시를 전달 MD로 검토 가능.
- [x] 전달 폴더에는 MD 한 개만 있고 별도 내부 캐시로 무결성·재사용 검사가 가능.
- [x] 새로운 출력 계약과 사람의 검토가 필요한 범위를 정리.

### R4. 평가 기준별 검색 배분 — P1 / M

- [x] 12개 항목의 조사표와 목적이 있는 질의를 만들고 단계별 예산을 배분한다.
- 수용 기준:
  - [x] 현행 설계대로 회차당 최대 2질의, 보완 최대 1회, 재시도를 포함해 전체 검색 6시도를 지킨다.
  - [x] 각 질의에 기술·대상 평가 기준·필요 출처·선정 이유가 있고 SW/HW 기회를 배분한다.
  - [x] 더 작은 입력 한도나 실패에도 전체 한도를 넘지 않고, 미조사 항목과 이유를 남긴다.
- 검증: 6/10/5 기본 한도, 0·축소 한도, 최초 검색 재시도, 보완 예약량, 한 기술의 후보만 있는 경우. 모델이 작성한 보완 질의의 중복 처리.
- 의존: R2~R3. 실행 순서상 R8·체크포인트 A 이후 착수.
- 예상 파일: `search_plan.py`, `schemas.py`, `node.py`, `tests/test_search_plan.py`, `tests/test_graph.py`.

### R5. 필요한 원문과 문단을 먼저 확보 — P1 / M

- [x] 평가 목적에 맞는 출처 우선순위와 질문별 문단 발췌를 적용한다.
- 수용 기준:
  - [x] 항목 관련성·직접성·출처 성격·중복·시점에 따른 선택 이유를 기록한다. 논문 URL 직접 확인도 원문 예산에 포함한다.
  - [x] 첫 12,000자 이후에 필요한 근거가 있는 사례에서도 관련 문단을 선택하며 앞뒤 조건·원문 위치·전체 사본을 보존한다.
  - [x] 조회 실패와 예산 소진을 구분하고, 공개 대체 URL을 조회하더라도 총 원문 10시도 및 하위 입력 한도를 지킨다.
- 검증: 메뉴가 긴 공식 자료, 본문 뒤쪽의 제한 조건, 낮은 순위의 관련 공식 자료, PDF 실패, 같은 URL·본문 중복, 접근 상태별 기록.
- 의존: R4.
- 예상 파일: `sources.py`, `tools.py`, `node.py`, `tests/test_sources.py`, `tests/test_tools.py`.

### R6. 확보 근거를 이용해 평가·보완 — P1 / M

- [x] 모델에 항목별 조사 상태와 관련 발췌를 주고 보완 필요성을 근거 중심으로 판단한다.
- 수용 기준:
  - [x] 각 결론의 대상이 RDKV·선정 논문·관련 제품군 중 무엇인지 명시되고, 시장 수치를 다른 범위로 대입하지 않는다.
  - [x] 기존 유효 결과는 보존하되 새 반대 근거가 있으면 충돌로 표시한다. 보완 질문은 해결 가능한 공백에 연결한다.
  - [x] 현재 남은 오류와 보완에서 해결된 오류를 구분하고 최종 상태·부분 실패 판정에는 현재 오류의 영향만 반영한다.
  - [x] 보완 최대 1회, 논리적 평가 최대 2회 및 전송 재시도를 포함한 LLM 상한을 유지한다.
- 검증: 기술군 자료만 있는 경우, 긍정·반대 근거 혼재, 숫자 단위 누락, 후속 모델 실패 시 부분 결과 보존.
- 의존: R3~R5.
- 예상 파일: `prompts.py`, `providers.py`, `node.py`, `tests/test_providers.py`, `tests/test_graph.py`.

### 체크포인트 B — 검색부터 평가까지

- [x] 고정 제공자로 12개 항목과 조사 상태가 반환됨.
- [x] 검색·원문·LLM 예산 및 보완 횟수 검사 통과.
- [x] 긍정 근거가 있는 사례와 실제로 부족한 사례를 모두 올바르게 처리.

### R9. 같은 기준으로 수정 전후 비교 — P2 / M

- [x] 오프라인 회귀 검사 후 동일 입력·기준일·모델·한도로 라이브 비교 실행 1회를 수행한다.
- 수용 기준:
  - [x] 통합용 전달 MD에 12개 담당 항목·필요한 한계·인용 근거가 있고, 나머지 보고서 절·내부 실행 기록은 섞이지 않는다.
  - [x] 정해둔 회귀 사례에서 근거 없는 exact 승격·임의 수치·예산 초과가 0건이다. 실세계 결과는 원문 대조와 남은 한계를 별도 기록한다.
  - [x] 호출·토큰 사용량, 원문 성공·실패, 직접·기술군 근거, 미확인 원인의 전후 차이를 기록한다. unknown 개수 감소를 합격 조건으로 사용하지 않는다.
- 검증: 전체 unittest, `compileall`, 실제 보고서의 긍정 주장 모두 원문 대조, 변경 파일 범위 확인.
- 의존: R1~R6 및 R8. R7의 상세 진단 확장은 필수 아님.
- 예상 파일: `tests/test_quality_regression.py`, `LIVE_VALIDATION.md`, `README.md`, `../docs/MARKET_AGENT_DESIGN.md`.

### R7. 회차별 내부 진단 저장 — 선택 / M

- [ ] 디버깅이 필요한 경우 최초·보완 결과와 해결 이력을 별도 내부 기록으로 보존한다. 통합용 전달 MD의 필수 작업은 아니다.
- 수용 기준:
  - [ ] 진단에 회차·단계·영향 항목·해결 상태가 있으며 최종 실패로 오해되지 않는다.
  - [ ] 중간 평가·발췌·검색 후보 기록은 디버깅 옵션에 한정하며 기본 전달물에 포함하지 않는다. 키는 기록하지 않는다.
  - [ ] 선택 기능을 꺼도 R6의 현재 오류 판별과 R8의 최소 저장·재사용 검사가 동작한다.
- 검증: 최초 오류가 보완에서 해결된 경우, 최종 조회 실패가 남은 경우, 옵션 유무에 따른 전달 MD·판정의 일치.
- 의존: R6.
- 예상 파일: `schemas.py`, `node.py`, `cli.py`, `tests/test_graph.py`, `tests/test_cli.py`.

### 체크포인트 C — 최종 결과

- [x] 필수 작업 R1~R6·R8·R9의 수용 기준·테스트·문서 갱신 완료. R7은 선택 기능으로 별도 완료 여부 표시.
- [x] 미확인 항목이 남아도 전달 MD에 판단 보류 이유가 있고 내부에서 다음 조사를 설명할 수 있음.
- [x] 실제 실행 결과와 확인하지 못한 범위를 사용자에게 보고.
- [x] 팀의 부모 Graph 전체 통합은 기존 별도 작업으로 유지.


## 수정 후 검증 — 2026-09-22

- 필수 R1~R6·R8·R9와 체크포인트 A~C 완료. R7은 `--debug` 모델 결과·Graph 이동 저장만 구현했으며 전체 해결 이력 확장은 선택 사항으로 남김.
- 테스트 58개, compileall, diff 형식 검사 통과. 실제 입력 원본·환경 파일은 유지.
- 라이브 세 실행의 합계: 검색 12 / 원문 28 / LLM 6. 마지막 실행: 4 / 8 / 2. 모든 실행은 각 입력 상한 이내.
- 마지막 원문·모델 결과를 API 추가 호출 없이 재검증. 최종 전달 폴더에는 `market_handoff.md`만 존재.
- 선정 기술 시장 판정 12개 미확인, 원문 대조한 HW 관련 제품군 정보 1건. 미확인 감소를 완료 조건으로 사용하지 않음.
- 세부 실행·토큰·원문 검토·한계: [LIVE_VALIDATION.md](../market_agent/LIVE_VALIDATION.md).
